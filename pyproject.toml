[project]
name = "learning-open-metadata"
version = "0.1.0"
description = ""
authors = [
    {name = "Airton Lira Junior",email = "airtonlirajr@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas (>=3.0.2,<4.0.0)",
    "sqlalchemy (>=2.0.49,<3.0.0)",
    "psycopg2-binary (>=2.9.12,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "kaggle (>=2.1.2,<3.0.0)"
]

[tool.poetry]
packages = [{include = "learning_open_metadata", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
